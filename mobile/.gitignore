# Credenciales y firma (NUNCA commitear)
credentials.json
*.keystore
*.jks
*.p8
*.p12
*.mobileprovision
# .npmrc SI se versiona: lleva legacy-peer-deps=true, sin el cual
# npm install falla por el conflicto de peers y el build de EAS no arranca.

# EAS / build config con datos sensibles
# eas.json SI se versiona: no lleva secretos (las credenciales van en
# credentials.json, que si esta ignorado) y define a que backend apunta cada build.
