import '@testing-library/jest-dom';
// Inicializa i18next (idioma por defecto: español) para que los componentes que
// usan t() rendericen texto real en los tests en vez de las claves crudas.
import './i18n';
