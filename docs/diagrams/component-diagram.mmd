flowchart TB
    classDef planned fill:#F5F5F5,stroke:#AAAAAA,color:#888888,stroke-dasharray:5 5

    subgraph Clients
        subgraph Browser["Web Browser"]
            Frontend["React SPA\nVite · Zustand · TanStack Query"]
        end
        subgraph Mobile["Mobile Device"]
            MobileApp["Expo App\nExpo Router · AuthContext"]
        end
    end

    subgraph Backend["Backend — Django 5.1 + DRF"]
        Auth["Authentication\n— JWT simplejwt —\naccess 2 h / refresh 1 d\nRoles: Admin · Salesperson · Bootcamper"]
        Leads["Leads"]
        Payments["Payments"]
        Programs["Programs"]
        Users["Users"]
        Notif["Notifications\nCelery tasks only"]
        Analytics["Analytics\nplanned"]:::planned
        Swagger["API Docs\ndrf-spectacular"]
    end

    subgraph Infra["Data & Infrastructure"]
        PG[("PostgreSQL 16")]
        Redis["Redis 7"]
        Celery["Celery Worker"]
        SMTP["SMTP Email\nGmail / any SMTP"]
        OCR["OCR Engine\nTesseract"]
    end

    subgraph Ext["External Services — planned"]
        S3["Object Storage\nS3 / MinIO"]:::planned
        GCal["Google Calendar API"]:::planned
    end

    %% Clients → Backend
    Frontend -->|REST + JWT| Auth
    Frontend --> Leads
    Frontend --> Payments
    Frontend --> Programs
    MobileApp -->|REST + JWT| Auth
    MobileApp --> Leads

    %% Backend → DB
    Auth --> PG
    Leads --> PG
    Payments --> PG
    Programs --> PG
    Users --> PG

    %% Backend → Async
    Auth -->|token blacklist| Redis
    Leads -->|enqueue tasks| Celery
    Payments -->|enqueue OCR| Celery
    Notif --> Celery

    %% Celery → infra
    Celery -->|broker / result| Redis
    Celery -->|send emails| SMTP
    Celery -->|parse receipts| OCR

    %% Planned
    Payments -.->|upload receipts - planned| S3
    Leads -.->|schedule calls - planned| GCal
