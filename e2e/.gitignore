node_modules/

# Tokens de sesión generados en cada corrida — nunca se versionan.
.auth/

# Artefactos de reporte
playwright-report/
test-results/
reports/

# Specs generados por bddgen a partir de features/*.feature + steps/*.js
# (playwright-bdd). Se regeneran con `npm run pretest:bdd` — no se versionan.
.features-gen/
